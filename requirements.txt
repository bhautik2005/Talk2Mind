# Talk2Mind — requirements.txt
# Tested combination (Python 3.10 / 3.11 recommended).
#
# GPU users: replace `tensorflow` below with the CUDA-matched wheel for your
# GPU/driver (see https://www.tensorflow.org/install/pip) instead of pip
# installing the generic one — versions are very driver-sensitive.

# cd talk2mind
# python -m venv venv

# Windows
#  .venv\Scripts\activate

# pip install -r requirements.txt


# tensorflow==2.16.1        # use tensorflow-cpu==2.16.1 if you have no GPU
# numpy==1.26.4
# pandas==2.2.2
# librosa==0.10.2.post1
# opencv-python-headless==4.10.0.84
# soundfile==0.12.1         # librosa's audio backend on Windows/Linux
# scikit-learn==1.5.0       # optional: metrics/splitting utilities
# tqdm==4.66.4              # optional: progress bars if you add them to train.py

# ---- Python 3.13 ----
tensorflow==2.20.0
numpy>=1.26,<3.0
pandas==2.2.3
librosa==0.10.2.post1
opencv-python-headless==4.10.0.84
soundfile==0.12.1
scikit-learn==1.5.2
tqdm==4.66.5